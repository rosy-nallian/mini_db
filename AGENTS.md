# Mini-DB 项目总规范

## 项目概述

Mini-DB 是《大型平台软件设计实习》课程项目，目标是分步构建一个简化数据库系统。项目贯通三门课程知识：

1. **compiler/（SQL 编译器）** —— 编译原理：词法分析 → 语法分析 → 语义分析 → 执行计划生成
2. **storage/（页式存储系统）** —— 操作系统：页分配/回收、页式读写、缓存与替换策略、磁盘持久化
3. **engine/（数据库系统）** —— 数据库：执行算子、存储引擎、系统目录、CLI 交互

数据流总览：

```
SQL 文本
  → [compiler] Token 流 → AST → 语义检查 → 逻辑执行计划(Plan)
  → [engine]   执行计划 → 算子(CreateTable/Insert/SeqScan/Filter/Project/Delete)
  → [storage]  页级读写(get_page/write_page) + 缓存(LRU/FIFO) + 磁盘文件
```

## 模块边界（绝对不可跨越）

| 模块 | 允许依赖 | 禁止依赖 |
|------|---------|---------|
| compiler/ | 无（仅标准库） | storage/, engine/ |
| storage/ | 无（仅标准库） | compiler/, engine/ |
| engine/ | compiler/, storage/ | 无 |

说明：
- compiler 只负责"把 SQL 翻译成计划"，**不得感知磁盘、页、缓存**。
- storage 只负责"按页存字节"，**不得感知 SQL、表、行语义**。
- engine 是唯一同时了解两侧、负责把计划翻译成页级访问的模块。

## 任务分级（P0 必做 / P1 进阶 / P2 扩展）

完整清单见 `docs/task-tiers.md`。**当前阶段只做 P0。**

| 级别 | 内容 | 状态 |
|------|------|------|
| **P0 必做** | Lexer+Token 位置、四类 SQL、AST、Catalog、语义检查、Logical Plan、错误定位、基础测试 | ✅ 唯一允许实现的 |
| **P1 进阶** | AND/OR/NOT、复杂表达式、≥2 条优化规则、智能错误诊断、Plan 可视化、隐藏测试通过率 | ⏸ 暂缓，需确认 |
| **P2 扩展** | JOIN/GROUP BY、Fuzz Testing、优化框架、代价模型、EXPLAIN、高级错误恢复 | 🚫 本课程不做 |

### P0 边界速查

- WHERE **仅支持单条件** `列 比较符 值`（比较符 `= <> != < <= > >=`）；不支持 AND/OR/NOT、算术、嵌套括号
- SELECT **仅单表**，无 JOIN / 子查询
- **不做任何优化**（无谓词下推、无常量折叠），计划按标准形状生成
- 具体要求与验收方式以 `docs/task-tiers.md` 为准

## 全局开发规则

0. **越界禁令**：任何不在 P0 清单内的功能，一律不得实现。发现"顺手能加"的 P1/P2 特性时，只记录到 `docs/task-tiers.md` 对应清单，等 P0 全绿并经确认后再动。宁可少做，不可多做。
1. **开发前必读**：开始某个子模块开发前，必须读取该子模块目录下的 AGENTS.md 及其父级 AGENTS.md。不需要读取其他无关模块的 AGENTS.md。
2. **修改后必更新**：每次修改代码后，如果接口、行为或职责发生变化，必须同步更新对应目录下的 AGENTS.md。
3. **接口契约优先**：模块间通信必须通过 `docs/interface-contract.md` 中约定的接口，不允许绕过接口直接访问。
4. **错误处理规范**：
   - 词法/语法/语义错误：输出 `[错误类型，位置，原因说明]`
   - 存储层错误：抛出异常，由上层捕获
   - 执行引擎错误：返回错误码或抛出异常，CLI 层统一处理
5. **测试要求**：每个模块完成时，必须在 tests/ 对应目录下编写测试用例。
6. **代码风格**：
   - 使用清晰的命名（动词开头的方法，名词开头的类）
   - 每个类/函数必须有简短的注释说明用途
   - 避免上帝类（单个类不超过 300 行）
7. **版本管理（分支策略与协作约束）**：

   ## 分支策略
   - 每个模块开发前，从主分支创建独立分支，分支命名格式：`feature/[模块名]`
     - 示例：`feature/compiler-lexer`、`feature/storage-cache`、`feature/engine-executor`
   - 在该分支上完成本模块的全部开发与测试
   - 模块完成并测试通过后，合并回主分支
   - 开发过程中，每完成一个可运行的功能点提交一次 commit，commit message 格式：`[模块名] 功能描述`

   ## 提交边界（必须严格遵守）
   - **只提交自己模块目录下的文件**，禁止提交以下内容：
     - ❌ 其他模块的代码文件
     - ❌ 其他模块的 AGENTS.md
     - ❌ docs/interface-contract.md（除非接口变更已与全组确认）
     - ❌ 根目录 AGENTS.md（除非全组讨论通过）
     - ❌ IDE 配置文件（.vscode/、.idea/ 等，应写入 .gitignore）
     - ❌ 编译产物、缓存文件、data/ 下的数据库文件
   - 允许提交的文件范围：
     - ✅ 本模块 src/ 目录下的代码
     - ✅ 本模块目录下的 AGENTS.md
     - ✅ tests/ 下与本模块对应的测试文件
     - ✅ README.md（仅限补充本模块的使用说明）

   ## 远程提交规则（必须严格遵守）
   - **每次 push 到远程仓库前，必须与全组成员确认**
   - 确认内容至少包括：
     1. 将要 push 的分支名
     2. 将要 push 的 commit 数量和内容概要
     3. 是否涉及公共文件（如 docs/interface-contract.md）的修改
   - 未经确认，禁止直接 push 到 main 分支或任何远程分支
   - 建议使用 Pull Request / Merge Request 流程进行合并

   ## 冲突处理规则（必须严格遵守）
   - **任何情况下，绝对禁止使用 `git push --force` 或 `git push -f`**
   - **绝对禁止使用 `git push --force-with-lease` 覆盖远程提交**
   - 遇到冲突时：
     1. 先 `git pull` 拉取远程最新代码
     2. 手动解决冲突文件
     3. 与相关模块负责人确认冲突解决方式是否正确
     4. 本地测试通过后，与全组确认，再正常 push
   - 如果无法确定如何解决冲突，停下来找队友当面/在线讨论，不要自行猜测

   ## 合并回 main 的流程
   1. 本模块所有测试通过
   2. 对应 AGENTS.md 已更新
   3. 与全组确认将要合并的内容
   4. 由分支创建者执行合并，或通过 PR 由他人 review 后合并
   5. 合并后删除该 feature 分支

## 开发顺序

1. 先写 `docs/interface-contract.md`，定义所有模块间接口
2. 开发 compiler/（词法 → 语法 → 语义 → 计划）
3. 开发 storage/（页 → 缓存 → 磁盘）
4. 开发 engine/（存储引擎 → 执行算子 → 系统目录）
5. 集成测试

## AI 协作规则

- 每次收到开发任务时，先确定目标模块，然后**只读取**该模块路径链上的 AGENTS.md（从根到目标目录）。
- 不要读取与当前任务无关的 AGENTS.md。
- 完成开发后，如果接口有变化，更新该模块的 AGENTS.md 和 `docs/interface-contract.md`。
